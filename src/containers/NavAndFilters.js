import React from 'react'
import Filters from '../components/Filters'
import Nav from '../components/Nav'

const NavAndFilters=()=>{
    return (
      <div>
        <Nav />
        <div style={{display:'flex'}}>
          <Filters />
          jdwqjodk;
        </div>
      </div>
    );
}

export default NavAndFilters
